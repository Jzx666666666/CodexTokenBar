# 安全策略

## 支持的版本

当前接受安全修复的版本系列为 `1.0.x`。更早版本不在支持范围内。

## 报告漏洞

若仓库已启用 GitHub Private vulnerability reporting，请优先通过该入口提交完整报告。该入口位于仓库的 Security 页面，报告内容不会作为公开 issue 发布。

若该入口不可用，本仓库目前没有可验证的私密联系渠道。此时只能创建一个不含漏洞细节的最小公开 issue，请求维护者建立私密沟通方式。公开 issue 中不要附上凭据、认证文件、载荷、日志、复现细节或利用方法。

## 保密要求

任何渠道都不允许公开张贴以下内容：

- Codex CLI 或 OpenAI 平台凭据、访问令牌或认证文件；
- 本地 `%LocalAppData%\CodexTokenBar` 中未脱敏的原始内容；
- 原始配额载荷（例如 `codex app-server` 返回的 JSON-RPC 响应全文）；
- 含本机用户名、完整路径或令牌的未脱敏日志片段。

请先脱敏，再提交。本仓库也不会接收或留存这类原始数据。

## 处理流程

维护者收到报告后会核实问题并评估影响范围；公开说明应等到修复可用且不会增加利用风险时再发布。版本变化记录在[更新日志](CHANGELOG.md)（`CHANGELOG.md`）。

## 边界说明

本应用只连接本机 `codex app-server` JSON-RPC 子进程，不直接连接远程配额端点，也不内置、分发 Codex CLI 或复制其凭据。这不表示 Codex CLI 完全离线；CLI 为完成认证和自身功能，可能按其设计与 OpenAI 服务通信。
